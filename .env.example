# MongoDB Atlas Connection URI
# Obtain from MongoDB Atlas -> Database -> Connect -> Drivers
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/sentinel?retryWrites=true&w=majority